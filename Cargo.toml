[package]
name = "alteraparser"
version = "0.1.0"
edition = "2021"
description = "A parser-combinator library with lexer, grammar, and AST support"
license = "MIT"

[dependencies]
regex = "1"
thiserror = "2"
